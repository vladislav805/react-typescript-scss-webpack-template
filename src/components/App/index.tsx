import * as React from 'react';
import './App.scss';

export default class App extends React.Component<unknown, unknown> {
    render() {
        return (
            <p>Hello, world!</p>
        );
    }
}
